<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update page</title>
    <style>
        p {font-size: 2rem; font-weight: bold;}
        label {display: flex; font-size: 1.5rem;}
        input {width: 250px; height: 30px; color: red;}
        button {width: 100px; height: 30px; font-size: 1rem;}
        .buttonSave {margin-top: 5%;}


    </style>
</head>
<body>
    <div style="margin-left: 40%;  display: inline-block;">
        <form action="/update" method="POST">
            <p>Update an user</p>
            <div>
              <label for="">First Name</label>
              <input type="text" name="firstName" value="<%= dataUser.firstName %>">
            </div>
            <div>
              <label for="">Last Name</label>
              <input type="text" name="lastName" value="<%= dataUser.lastName %>">
            </div>
            <div>
              <label for="">Email</label>
              <input type="email" name="email" value="<%= dataUser.email %>">
            </div>
            <div>
              <label for="">Address</label>
              <input type="text" name="address" value="<%= dataUser.address %>">
            </div>
            <input type="text" name="id" value="<%= dataUser.id %>" hidden>
            <!-- ID field dont need update, but must to have in order to get ID. So it have to hidden -->
            <button 
              type="submit" 
              class="buttonSave">
                Save
            </button>
          </div>
      </form>
    </div>
</body>
</html>